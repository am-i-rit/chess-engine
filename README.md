# Chess Engine
A chess engine with an SDL3-based GUI, written in C++.

## Status
Early development — currently just have the SDL3 window/render loop working. Board representation, move generation, and the actual chess logic are next.

## Prerequisites
CMake (3.20+)
A C++17 compiler (clang++ on macOS, gcc on Linux, MSVC on Windows)
SDL3
